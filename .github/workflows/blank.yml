name : simple hello world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - name: checkout the code
         uses: actions/checkout@v4
       - name: print the file
         run: psw

         
