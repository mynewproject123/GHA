name : simple hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - name: build step
         run: echo "build step"
  test:
      runs-on: ubuntu-latest
      steps:
        - name: test
          run: echo "test job"
  deploy:
     runs-on: ubuntu-latest
     steps:
       - name: deploy stage
         run: echo "deploy step"
         
